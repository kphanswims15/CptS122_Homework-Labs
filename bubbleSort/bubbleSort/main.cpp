#include "bubbleSort.h"

int main(void)
{
	cout << "U" << endl;
	/*vector<int> data = create_vector();
	vector<int>::iterator pos;

	cout << "Before: " << endl;

	for (pos = data.begin(); pos != data.end(); ++pos)
	{
		cout << *pos << ' ';
	}
*/
	/*float time = do_bubblesort(data, 10);

	cout << "Time it took" << time << endl;

	cout << "After:" << endl;

	for (pos = data.begin(); pos != data.end(); ++pos)
	{
		cout << *pos << ' ';
	}
*/
	return 0;
}